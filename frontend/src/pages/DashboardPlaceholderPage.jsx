function DashboardPlaceholderPage({ title }) {
  return (
    <section>
      <h1>{title}</h1>
      <p>Layout is ready. Attach functional content to this route once the corresponding screen is available.</p>
    </section>
  )
}

export default DashboardPlaceholderPage
